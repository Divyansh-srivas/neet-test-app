import React, { useState } from 'react';
import { render, fireEvent } from '@testing-library/react';
import { JSDOM } from 'jsdom';

// We don't have testing-library installed. I will just do a tiny vanilla React test.
